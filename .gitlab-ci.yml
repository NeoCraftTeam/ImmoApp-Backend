stages:
  - test
  - deploy

# Configuration globale pour utiliser UNIQUEMENT votre Runner VPS (0 minute GitLab consommée)
default:
  tags:
    - self-hosted-shell

# --- ÉTAPE 1 : TESTS ---
test_suite:
  stage: test
  script:
    - echo "🧪 Lancement des tests sur le VPS..."
    # On prépare un environnement de test local sur le VPS
    - cp .env.example .env.testing
    - php artisan key:generate --env=testing
    - composer install --no-ansi --no-interaction
    # Note : Assurez-vous d'avoir configuré une DB de test sur le VPS (ex: immo_testing)
    - php artisan test --env=testing
  only:
    - branches
    - merge_requests

# --- ÉTAPE 2 : DÉPLOIEMENT ---
production_deploy:
  stage: deploy
  script:
    - echo "🚀 Déploiement local sur le VPS..."
    - cd /var/www/ImmoApp-Backend || exit 1
    # On s'assure d'être sur la bonne branche avant le pull
    - git fetch origin
    - git checkout main
    - bash deploy.sh
  only:
    - main
